build site:
  image: node:latest
  stage: build
  script:
    - npm install --progress=false
    - npm install -g @vue/cli
    - cd project
    - npm install @vue/cli-service
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist


